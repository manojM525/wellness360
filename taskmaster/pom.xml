<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>
	<parent>
		<groupId>org.springframework.boot</groupId>
		<artifactId>spring-boot-starter-parent</artifactId>
		<!-- Bumped from 3.2.11 (EOL Dec 2024) to 3.5.16 — the final OSS patch
		     on the 3.5 line before it too reached EOL on June 30, 2026.
		     Closes CVE-2026-40973 (spring-boot itself; fix shipped in 3.5.14).
		     Chosen over jumping to Boot 4.1 to keep this a same-major-line,
		     low-risk minor-version bump (Spring Framework 6.x / Jakarta EE 10
		     throughout). Boot 4.1 migration is tracked separately as
		     deliberate follow-up work, not done here under a CI-fix deadline. -->
		<version>3.5.16</version>
		<relativePath/> <!-- lookup parent from repository -->
	</parent>
	<groupId>com.taskmaster</groupId>
	<artifactId>taskmaster</artifactId>
	<version>0.0.1-SNAPSHOT</version>
	<name>Task Management System</name>
	<description>Internship Case Study Project for Task Management System using Spring Boot</description>
	<url/>
	<licenses>
		<license/>
	</licenses>
	<developers>
		<developer/>
	</developers>
	<scm>
		<connection/>
		<developerConnection/>
		<tag/>
		<url/>
	</scm>
	<properties>
		<java.version>17</java.version>

		<!-- ===================================================================
		     EXPLICIT CVE OVERRIDES — NOT DECORATIVE.
		     Spring Boot's dependency BOM is a frozen snapshot once a line goes
		     EOL (3.5 did, June 30 2026), so a parent-version bump alone isn't
		     guaranteed to carry every upstream CVE fix. These properties force
		     Maven to resolve the transitive Tomcat/Jackson jars to the exact
		     patched versions Trivy confirmed close every HIGH/CRITICAL finding,
		     independent of what the frozen 3.5.x BOM happens to pin.
		     (Thymeleaf's override was removed below — see dependency removal.)
		     =================================================================== -->

		<!-- Fixes CVE-2026-41293 (HTTP/2 header validation, CRITICAL),
		     CVE-2026-43512, CVE-2026-43515, CVE-2025-48989, CVE-2025-52520,
		     CVE-2025-53506, CVE-2025-55752, CVE-2026-24734, CVE-2026-24880,
		     CVE-2026-34483, CVE-2026-41284, CVE-2026-42498, CVE-2026-43513.
		     10.1.55 is the maximum "fixed version" across all of the above. -->
		<tomcat.version>10.1.55</tomcat.version>

		<!-- Fixes CVE-2026-54512 / CVE-2026-54513 (jackson-databind
		     PolymorphicTypeValidator bypass -> arbitrary code execution).
		     Reachable from the internet-facing /tasks endpoint since Spring
		     Data REST serializes/deserializes every request through Jackson. -->
		<jackson-bom.version>2.18.8</jackson-bom.version>

		<!-- Fixes CVE-2025-41249 (annotation detection vulnerability).
		     Explicit pin as defense-in-depth even though 3.5.16 likely
		     already carries this fix (disclosed well before the EOL freeze). -->
		<spring-framework.version>6.2.11</spring-framework.version>
	</properties>
	<dependencies>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-data-jpa</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-data-rest</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-hateoas</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-web</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-actuator</artifactId>
		</dependency>
		<dependency>
			<groupId>io.micrometer</groupId>
			<artifactId>micrometer-registry-prometheus</artifactId>
			<scope>runtime</scope>
		</dependency>
		<!-- REMOVED: org.springframework.data:spring-data-rest-hal-explorer
		     Its browsable /tasks UI is rendered via a Thymeleaf fragment. With
		     Thymeleaf itself removed below (confirmed unused — no @Controller,
		     no templates/ dir, app is pure Spring Data REST JSON), keeping this
		     would either silently fail to render or drag Thymeleaf back in as
		     a transitive dependency, undoing the point of removing it. Add
		     both back together, deliberately, if a browsable demo UI is ever
		     wanted for recruiter walkthroughs — this is a feature decision,
		     not a bug fix, so it shouldn't ride along with a security patch. -->
		<!-- REMOVED: org.springframework.boot:spring-boot-starter-thymeleaf
		     Confirmed unused via `find src/main -iname "*.html"` and
		     `grep -rl "@Controller\b" src/main/java` — both empty. This app
		     serves HAL+JSON exclusively through Spring Data REST; there is no
		     view-rendering code path. Removing the dependency (rather than
		     patching it to 3.1.5.RELEASE) eliminates the SSTI attack surface
		     entirely instead of leaving a patched-but-present, unused engine
		     in the runtime image — smaller blast radius if a future CVE lands
		     in a code path nothing calls. -->
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-devtools</artifactId>
			<scope>runtime</scope>
			<optional>true</optional>
		</dependency>
		<dependency>
			<groupId>com.mysql</groupId>
			<artifactId>mysql-connector-j</artifactId>
			<scope>runtime</scope>
		</dependency>
		<dependency>
			<groupId>org.projectlombok</groupId>
			<artifactId>lombok</artifactId>
			<optional>true</optional>
		</dependency>
		<dependency>
			<groupId>net.logstash.logback</groupId>
			<artifactId>logstash-logback-encoder</artifactId>
			<version>7.4</version>
		</dependency>
		<dependency>
			<groupId>com.h2database</groupId>
			<artifactId>h2</artifactId>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-test</artifactId>
			<scope>test</scope>
		</dependency>
	</dependencies>

	<build>
		<plugins>
			<plugin>
				<groupId>org.springframework.boot</groupId>
				<artifactId>spring-boot-maven-plugin</artifactId>
				<configuration>
					<excludes>
						<exclude>
							<groupId>org.projectlombok</groupId>
							<artifactId>lombok</artifactId>
						</exclude>
					</excludes>
				</configuration>
			</plugin>
		</plugins>
	</build>

</project>